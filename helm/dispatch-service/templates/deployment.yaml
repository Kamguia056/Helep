apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dispatch-service.fullname" . }}
  labels: {{- include "dispatch-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: {{- include "dispatch-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "dispatch-service.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8003"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: dispatch-service
          image: "{{ .Values.global.imageRegistry | default "" }}{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8003
              protocol: TCP
          env:
            - name: SERVICE_PORT
              value: "8003"
            - name: KAFKA_BOOTSTRAP
              value: {{ .Values.global.kafkaBootstrap | quote }}
            - name: DB_PATH
              value: /data/dispatch.db
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.jwtSecretName }}
                  key: JWT_SECRET
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 10
          resources: {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "dispatch-service.fullname" . }}-pvc
